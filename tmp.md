### 5.3.3 ChatCore 进程设计

作为 Draughts 系统中的报文与业务执行层，ChatCore 负责将第三章提出的匿名通信机制与第四章提出的路径长度控制方法落地为可运行的协议处理流程。相较于前文的原理分析，本节重点讨论工程实现中的进程内协作方式、可交换加解密实现、报文字段语义扩展以及会话状态维护策略，突出系统实现阶段特有的状态组织与一致性控制逻辑。

#### 5.3.3.1 架构设计

ChatCore 进程内架构如图 5-4 所示。整体采用以 `io_context` 为核心的事件驱动模型，UDP 报文收发、路由处理、会话维护与定时清理都在同一事件循环上下文内推进，以保证状态更新的时序一致性。命令行交互由独立线程负责输入采集，随后通过异步投递进入事件循环执行，从而避免交互线程直接写入网络状态，降低并发访问导致的状态撕裂风险。

在该架构中，业务处理由四个核心模块协同完成。报文处理模块负责请求构造、随机游走阶段推进、出口交付判定和回复回程转发，是协议状态机的执行中心；拓扑状态模块负责维护活跃邻居视图、两跳缓存和节点目录映射，为转发阶段提供可用的下一跳与下下跳候选；会话管理模块负责发起方会话与响应方缓存条目的生命周期维护，并通过周期清理控制状态规模；加解密模块负责逐跳参数层、地址层与载荷层的分层变换，保证不同处理阶段的信息可见范围符合匿名通信要求。需要强调的是，定时维护逻辑并未独立成第五模块，而是分别内聚于拓扑状态与会话管理两个模块内部，与其状态职责保持一致。

ChatCore 与外部系统通过两条通道协作。其一是面向 TopoDaemon 的本地 IPC 通道，用于获取 `PLAN/HISTORY/STATE/TWOHOP` 结果，从而将拓扑维护与报文执行解耦；其二是面向远端节点的 UDP 数据通道，用于承载固定长度匿名报文。前者提供“路由上下文”，后者承担“数据平面转发”，二者在架构层面形成明确边界。

对应的关键类关系如图 5-5 所示。类图反映出 ChatCore 的核心并非单类实现，而是由 `DraughtsApp`、`DraughtsNode` 与 `TopodClient` 构成三元协作结构：`DraughtsApp` 执行报文状态机，`DraughtsNode` 管理本地拓扑状态与目录映射，`TopodClient` 作为本地 IPC 桥接层提供路由与历史视角查询。`Cli` 仅作为控制入口连接 `DraughtsNode` 与 `DraughtsApp`，不持有业务状态；`Config` 仅提供参数边界，不参与运行期状态演进。

从类图到实现逻辑的映射可以概括为三条执行链。第一条是命令执行链：CLI 读取命令后异步投递，进而驱动拓扑查询、消息发送、会话回复等动作在统一事件上下文中执行。第二条是路由协作链：报文处理阶段需要的下一跳与下下跳由 IPC 查询提供，查询结果再与本地目录和两跳缓存联合使用，形成“外部路由结果 + 本地状态校验”的协同机制。第三条是状态闭环链：`DraughtsPacket` 负责跨节点携带阶段参数与会话标识，`PeerDescriptor` 负责节点身份、公钥与地址绑定，`ResponderLru` 负责待回复上下文缓存，这些结构共同支撑请求、转发、交付与回复的完整闭环。由此可见，类图中的静态关联能够直接解释系统运行期的动态处理路径，而非仅是代码组织关系。

#### 5.3.3.2 可交换加密算法的实现

ChatCore 的可交换加解密实现采用“共享密钥协商 + 密钥派生 + 对称变换”的分层组合。节点之间首先基于椭圆曲线密钥协商生成共享秘密，再通过 HKDF 派生会话所需的对称密钥与初始化向量，最后使用流式对称变换对目标字段执行可逆处理。该实现与数据包字段组织相对独立，其关注点在于如何构造可叠加、可剥离、可逆的变换过程，以支持多跳路径上的分层处理需求。

可交换性来自“分层独立变换”的处理思想。对于同一数据块，不同阶段可叠加多个密钥域对应的变换层；后续节点只需对自身可见层执行逆向处理，即可恢复本阶段所需语义，而不会暴露与本跳无关的信息。这一机制使中继节点具备“最小可见”特性，即只处理本跳必要信息，不获取端到端明文。

在工程实现中，系统进一步将密钥生命周期划分为身份级、会话级与跳级三个作用域。身份级密钥用于节点长期身份和稳定协商基准，会话级临时密钥用于同一会话的端到端安全上下文，跳级临时密钥用于逐跳参数层与地址层的重封装。通过该分域设计，节点在任意时刻只依赖当前处理阶段所需密钥材料即可完成字段变换，既降低了状态耦合，也减少了全路径密钥驻留带来的暴露面。

#### 5.3.3.3 数据包结构及处理语义扩展

相较于第三章的协议抽象，工程实现阶段对数据包的核心扩展集中在 `x` 与 `term`（实现字段名为 `topo_term`）两个字段。前者承担阶段控制职责，后者承担拓扑语义一致性职责。第四章已经给出 `x` 的迭代原理，本节仅说明其在协议状态机中的实现语义。

在运行过程中，`x` 不仅作为继续转发概率的输入，还承担阶段编码功能。系统通过 `x` 的符号和取值区间区分请求随机游走、出口交付、回复引导和回程续转发等状态，并在每一跳处理后写回新的 `x`。因此，报文是否继续扩展路径、何时进入出口阶段以及何时切换到回程流程，均可在同一控制字段上完成一致判定。

`term` 的引入主要用于解决邻居动态变化导致的路由语义漂移。以三节点场景为例：节点 A 收到数据包时，包内携带 `term=m`，且当前下一跳字段指向节点 B。此时 A 不直接基于“自己此刻最新邻居表”选择下下跳，而是先以 `m` 为上下文去检索“B 在该版本下的两跳视角快照”。若该上下文下 B 存在可用快照，则在相关快照中选取版本号最大的记录，记为 `term=n`，再从该记录对应的邻居列表中选择下下跳 C。

完成选择后，A 将数据包中的下下跳信息写为 C，并将包内 `term` 从 `m` 回填为 `n`，随后再执行后续封装与转发。这样，B 在下一跳处理该包时将处于与 A 一致的拓扑语义上下文，路径推进遵循“历史上下文连续性”，而不是被瞬时最新视图强制打断，从而显著降低邻居抖动场景下的路径错位与回程失配问题。

此外，工程实现中还引入了字段回填与再封装过程。即在完成阶段判定和下一跳选择后，系统先更新 `x` 与 `term`，再执行对应层级的重封装并转发。该顺序保证每跳输出报文都携带可被下一跳正确解释的阶段状态和拓扑上下文，是系统稳定运行的重要细节。

#### 5.3.3.4 会话状态及参数维护

ChatCore 的会话机制用于支撑匿名回复与连续对话。系统采用“发起方会话表 + 响应方缓存池”的双轨状态组织方式。发起方以 `session_id` 为键维护会话条目，保存会话临时密钥、目标对端信息与最近使用时间，用于请求发送后在回复到达时恢复解密上下文，并支持同一会话下的后续消息复用。响应方则维护待回复缓存条目，条目中保存回程入口地址、阶段公钥参数、地址层片段与拓扑上下文等信息，用于支持“先接收请求、后发送回复”的异步交互场景。

响应方缓存采用 LRU 链和哈希索引联合组织。该结构在保持查询效率的同时，能够按时序淘汰旧条目，避免长期运行造成缓存无界增长。与只保留单条会话状态的简化实现不同，本系统允许同一 `session_id` 关联多条待回复记录，从而适配延迟回复和多轮应答。

会话复用机制是工程实现中的关键优化。首次发送建立会话后，后续消息可复用既有会话参数，仅更新业务载荷与当前阶段相关字段；当回复到达时，系统基于 `session_id` 恢复会话上下文并完成交付，从而维持对话连续性并减少重复建链开销。与此同时，系统通过定时清理和有效性校验控制状态质量：发起方会话按 TTL 过期回收，响应方缓存按容量上限执行淘汰；对于标识无效、字段缺失或上下文不一致的条目，系统采取保守处理并记录诊断日志，防止异常状态继续进入后续转发路径。

#### 本节小结

本节从系统实现视角给出了 ChatCore 的关键设计：以 `io_context` 为中心的事件驱动架构、与 TopoDaemon 解耦的路由协作方式、可交换加解密的分域实现、`x/term` 驱动的阶段与一致性语义，以及可复用的会话状态维护机制。通过上述设计，ChatCore 能够在动态拓扑环境下稳定完成匿名请求与匿名回复闭环，并保持良好的可观测性与可维护性。
